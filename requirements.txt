# Wispr-Lite pinned dependencies
# Compatible with Linux Mint 21.3/22 (Python 3.10+)

# Core dependencies
faster-whisper==1.0.3
sounddevice==0.4.7
webrtcvad==2.0.10
PyGObject==3.42.2
pynput==1.7.7
python-xlib==0.33
PyYAML==6.0.1
numpy==1.24.4
dbus-python==1.3.2

# Transitive dependencies (auto-installed with above)
# ctranslate2 (via faster-whisper)
# huggingface-hub (via faster-whisper)
# tokenizers (via faster-whisper)
# cffi (via sounddevice)
